# SDK 版本升级计划

## 当前版本 vs 最新版本对比

### 1. AFNetworking

- **当前版本**: 4.0.1
- **最新版本**: 4.0.1 (已停止维护)
- **状态**: ⚠️ 已停止维护，最新版本可能仍未包含隐私清单文件
- **建议**:
  - 继续使用当前版本，通过 Podfile 的 post_install hook 手动添加隐私清单文件（已实现）
  - 或考虑迁移到 Alamofire（Swift）或其他维护良好的网络库

### 2. SDWebImage

- **当前版本**: 5.9.5
- **最新版本**: 5.18.0+ (包含隐私清单文件)
- **状态**: ✅ 最新版本已包含隐私清单文件
- **建议**: 升级到 5.18.0 或更高版本
- **升级命令**:

  ```ruby
  # 在 WuKongBase.podspec 中修改
  s.dependency 'SDWebImage','~> 5.18.0'
  ```

### 3. MBProgressHUD

- **当前版本**: 1.1.0
- **最新版本**: 需要检查
- **状态**: ⚠️ 需要检查最新版本是否包含隐私清单文件
- **建议**: 检查最新版本，如果包含则升级

### 4. FMDB

- **当前版本**: 2.7.5
- **最新版本**: 需要检查
- **状态**: ⚠️ 需要检查最新版本是否包含隐私清单文件
- **建议**: 检查最新版本，如果包含则升级

### 5. Starscream

- **当前版本**: 3.1.1
- **最新版本**: 需要检查
- **状态**: ⚠️ 通过 Socket.IO-Client-Swift 依赖，需要检查最新版本
- **建议**: 检查最新版本，如果包含则升级

### 6. Toast

- **当前版本**: 4.0.0
- **最新版本**: 需要检查
- **状态**: ⚠️ 需要检查最新版本是否包含隐私清单文件
- **建议**: 检查最新版本，如果包含则升级

## 升级步骤

### 方案 1: 升级到最新版本（推荐）

如果最新版本已包含隐私清单文件，这是最简单的解决方案：

1. **更新 WuKongBase.podspec**:

   ```ruby
   s.dependency 'SDWebImage','~> 5.18.0'  # 从 5.9.1 升级到 5.18.0
   # 其他库根据检查结果升级
   ```

2. **运行 pod update**:

   ```bash
   pod update SDWebImage
   # 或更新所有依赖
   pod update
   ```

3. **移除 Podfile 中的手动添加隐私清单文件的代码**（如果 SDK 已包含）

### 方案 2: 保持当前版本 + 手动添加（当前方案）

如果最新版本仍未包含隐私清单文件，或升级可能引入兼容性问题：

1. **保持当前版本不变**
2. **继续使用 Podfile 的 post_install hook 自动添加隐私清单文件**（已实现）
3. **优点**: 不需要修改代码，兼容性风险低
4. **缺点**: 需要维护手动添加的代码

## 检查最新版本是否包含隐私清单文件的方法

1. **查看 GitHub 仓库**:
   - 访问库的 GitHub 仓库
   - 查看最新 release/tag
   - 检查是否包含 `PrivacyInfo.xcprivacy` 文件

2. **使用 CocoaPods**:

   ```bash
   pod trunk info <库名>
   ```

3. **检查已安装的 Pod**:

   ```bash
   # 临时安装最新版本
   pod try <库名>
   # 检查是否包含 PrivacyInfo.xcprivacy
   ```

## 推荐升级顺序

1. **SDWebImage** (优先级最高)
   - 已知 5.18.0+ 包含隐私清单文件
   - 升级风险较低

2. **其他库** (按需升级)
   - 先检查最新版本是否包含隐私清单文件
   - 如果包含，可以考虑升级
   - 如果不包含，继续使用当前方案（手动添加）

## 注意事项

1. **兼容性测试**: 升级后需要充分测试，确保功能正常
2. **依赖关系**: 注意其他依赖库的版本要求
3. **代码变更**: 某些库的新版本可能有 API 变更
4. **回退方案**: 如果升级后出现问题，可以回退到当前版本

## 当前状态

✅ **已完成**: 通过 Podfile 的 post_install hook 自动为所有 6 个 SDK 添加隐私清单文件
⚠️ **可选**: 升级 SDWebImage 到 5.18.0+ 以使用官方提供的隐私清单文件
